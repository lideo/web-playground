{% extends "layout.njk" %}

{% block content %}
    <h1>{{ title }}</h1>

    <ul>
        {% for project in projectList %}
            <li><a href="{{ project.url }}">{{ project.name }}</a></li>
        {% else %}
            <li>There are no projects.</li>
        {% endfor %}
    </ul>
{% endblock %}
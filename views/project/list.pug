extends ../layout

block content
    h1= title

    ul
        each project in projectList
            li
                a(href=project.url) #{project.name}
        else
            li There are no projects.